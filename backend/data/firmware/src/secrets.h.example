#pragma once

// Copie este arquivo para secrets.h e preencha (secrets.h não deve ir ao git com senha real).
// USAGE_URL: IP LAN do Mac onde o coletor está rodando — nunca 127.0.0.1 na ESP32.
// O firmware escuta SSE em /events (troca o path /usage → /events).

#define WIFI_SSID "SUA_REDE"
#define WIFI_PASSWORD "SUA_SENHA"
#define USAGE_URL "http://192.168.1.10:8787/usage"
